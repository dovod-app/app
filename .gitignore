*.db
*.db-shm
*.db-wal
*.log
frontend/node_modules
frontend/package-lock.json
frontend/.nuxt/*
frontend/dist/*
frontend/.output/*
frontend/storybook-static
internal/api/static/*
!internal/api/static/.gitkeep
bin/*
# Claude Code
.claude/settings.local.json
.claude/worktrees/

# A session's own harness — preview configs, probe scripts, handoff notes.
# The two design specs already tracked under .scratch/ stay; these are local by
# nature and would only rot in the history.
.scratch/preview/
.scratch/handoff-*.md
